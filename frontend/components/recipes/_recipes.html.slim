= javascript_pack_tag 'recipes_list'
.container
  = link_to t('recipes.add'), new_recipe_path, class: 'btn btn-primary btn-sm with_bottom_margin'
  = react_component('RecipesList', locale: I18n.locale.to_s, access_token: JwtService.new.json_response(user: Current.user)[:access_token])