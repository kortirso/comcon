.container
  = javascript_pack_tag 'guild_form'
  = react_component('GuildForm', guild_id: @guild.id, locale: I18n.locale.to_s, access_token: JwtService.new.json_response(user: Current.user)[:access_token])
