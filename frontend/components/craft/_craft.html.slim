= javascript_pack_tag 'craft'
.inside
  .inside_topbar
    .title= t('craft.title')
  main
    #craft
      p= t('craft.description')
      = react_component('Craft', locale: I18n.locale.to_s, access_token: Current.user&.access_token)
