.mailer
  .head
    h1= t('mailer.user.confirmation_email.h1')
    p= t('mailer.user.confirmation_email.p1')
    p
      span= t('mailer.user.confirmation_email.click')
      span= link_to t('mailer.user.confirmation_email.confirmation_page'), "#{ENV['ROOT_URL']}/confirmations?email=#{EmailHelper.formatted_email(@user.email)}&token=#{@user.confirmation_token}"
    p= t('mailer.user.confirmation_email.buy')