Hello!

Someone set this email for change password to Docflix

If it wasn’t you, please ignore this message

Click this link <%= "#{ENV['ROOT_URL']}/#{'ru/' if I18n.locale == :ru}/users/new_password?email=#{EmailHelper.formatted_email(@user.email)}&reset_password_token=#{@user.reset_password_token}" %>

<%= t('mailer.user.confirmation_email.buy') %>
