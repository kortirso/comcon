<%= t('mailer.user.confirmation_email.h1') %>

<%= t('mailer.user.confirmation_email.p1') %>

<%= t('mailer.user.confirmation_email.click') %> <%= "#{ENV['ROOT_URL']}/#{'ru/' if I18n.locale == :ru}email_confirmations?email=#{EmailHelper.formatted_email(@user.email)}&confirmation_token=#{@user.confirmation_token}" %>

<%= t('mailer.user.confirmation_email.buy') %>
